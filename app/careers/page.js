import { StaticPage } from '@/components/StaticPage'

export const metadata = {
  title: 'Careers at ProCreators | Join Our Team',
  description: 'Join the ProCreators team. We\'re looking for talented individuals to help us build the future of content creation.',
}

export default function CareersPage() {
  return <StaticPage pageId="careers" />
}
